# Internal Documents

Place your internal/confidential PDF documents here (e.g., HR policies, financial reports), then run:

```bash
python main.py --reingest
```

Documents in this folder are **only** accessible to the `internal` API role.
